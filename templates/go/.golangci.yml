run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - revive
    - gofmt
    - goimports
    - unused

issues:
  exclude-dirs:
    - vendor
