# Placeholder Dockerfile so `cloudbuild.yaml`'s `docker build .` has something to build.
# Replace this with your actual application's build — this stage intentionally does nothing.
FROM alpine:3.21
CMD ["echo", "Replace this Dockerfile with your project's actual build."]
