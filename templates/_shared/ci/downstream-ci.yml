name: CI

# Starter CI workflow for downstream repositories that adopt this boilerplate.
# Uncomment the job(s) matching your stack and delete the rest.
# This file does not check for this boilerplate's own files (see .github/workflows/ci.yml
# in Soku-Convention-Boilerplate for that repository-hygiene check).

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  # javascript-typescript-node:
  #   name: JS/TS/Node
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "22"
  #     - run: npm ci
  #     - run: npm run typecheck
  #     - run: npm test

  # python:
  #   name: Python
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.11"
  #     - run: pip install -e . pytest ruff mypy
  #     - run: ruff check .
  #     - run: mypy .
  #     - run: pytest

  # go:
  #   name: Go
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-go@v5
  #       with:
  #         go-version: "1.22"
  #     - run: go vet ./...
  #     - run: go test ./...

  # java-spring:
  #   name: Java/Spring
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with:
  #         distribution: temurin
  #         java-version: "21"
  #     - run: mvn -B verify
